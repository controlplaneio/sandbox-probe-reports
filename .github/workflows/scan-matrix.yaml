name: scan-matrix

on:
  workflow_dispatch:
    inputs:
      profile:
        description: "nono registry profile to attest (<namespace>/<name>[@version])"
        required: false
        default: nolabs-ai/codex
  push:
    branches: ["matrix/**"]
  schedule:
    - cron: "0 8 * * 1" # weekly

permissions:
  contents: read

concurrency:
  group: scan-matrix-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: build (${{ matrix.goos }}/${{ matrix.goarch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - { runner: ubuntu-latest,  goos: linux,   goarch: amd64, ext: "" }
          - { runner: macos-latest,   goos: darwin,  goarch: arm64, ext: "" }
          - { runner: windows-latest, goos: windows, goarch: amd64, ext: ".exe" }
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v7.0.1
      # Cache the built binary keyed on the pin; workflow/script/doc-only changes then
      # reuse it instead of recompiling every run. The pin is go.mod's `tool` directive,
      # so go.mod/go.sum are the only inputs that can change the binary.
      - id: cache
        uses: actions/cache@v4
        with:
          path: sandbox-probe${{ matrix.ext }}
          key: probe-${{ matrix.goos }}-${{ matrix.goarch }}-${{ hashFiles('go.mod', 'go.sum') }}
      - if: steps.cache.outputs.cache-hit != 'true'
        uses: actions/setup-go@v7
        with: { go-version-file: go.mod }
      # Built from the module pinned in go.mod — not from a sibling checkout. darwin is built
      # on macos-latest and windows on windows-latest so cgo-free cross-compilation bugs in the
      # probe cannot silently produce a binary that will not run on its own target.
      - if: steps.cache.outputs.cache-hit != 'true'
        shell: bash
        run: GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} go build -o sandbox-probe${{ matrix.ext }} github.com/controlplaneio/sandbox-probe/v6
      - uses: actions/upload-artifact@v7
        with:
          name: sandbox-probe-${{ matrix.goos }}-${{ matrix.goarch }}
          path: sandbox-probe${{ matrix.ext }}

  # One row per harness × OS. `family` drives setup, `harness` is the job/report name. Every agent
  # runs via the general mock (scripts/mock-agent-api.mjs) with the model stubbed — no key, no
  # tokens. To add a harness: add rows below + a family-gated setup/run step, and (if it confines
  # the probe) an `expect` array of the sandbox_detection values that prove it. The assert is
  # data-driven off `expect`, so nothing else changes.
  scan:
    name: ${{ matrix.harness }} (${{ matrix.os }})
    needs: build
    # Best-effort opt-in: a row may set `optional: true` to be non-blocking (e.g. a fragile new
    # runtime being stabilised). All rows are currently blocking; none set it.
    continue-on-error: ${{ matrix.optional == true }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # direct — unconfined baseline on the runner.
          - { os: linux,   runner: ubuntu-latest,  arch: linux-amd64,   ext: "",     family: direct, harness: direct }
          - { os: macos,   runner: macos-latest,   arch: darwin-arm64,  ext: "",     family: direct, harness: direct }
          - { os: windows, runner: windows-latest, arch: windows-amd64, ext: ".exe", family: direct, harness: direct }
          # claude — real binary, model stubbed; as-is vs its own sandbox (bwrap on Linux, Seatbelt on macOS).
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: claude, harness: claude,         sandbox: "off" }
          # spec #6: the wrapper now resolves here (it read `unknown` before), so this row asserts the
          # name rather than falling back to accepting the kernel mechanisms it adds.
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: claude, harness: claude-sandbox, sandbox: "on", expect: '["bubblewrap"]' }
          - { os: macos, runner: macos-latest,  arch: darwin-arm64, ext: "", family: claude, harness: claude,         sandbox: "off" }
          - { os: macos, runner: macos-latest,  arch: darwin-arm64, ext: "", family: claude, harness: claude-sandbox, sandbox: "on", expect: '["seatbelt"]' }
          # codex — real binary, model stubbed; as-is vs its workspace-write sandbox (bwrap on Linux, Seatbelt on macOS).
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: codex, harness: codex,         sandbox: "off" }
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: codex, harness: codex-sandbox, sandbox: "on", expect: '["bubblewrap","no-new-privs","seccomp-filter","landlock"]' }
          - { os: macos, runner: macos-latest,  arch: darwin-arm64, ext: "", family: codex, harness: codex,         sandbox: "off" }
          - { os: macos, runner: macos-latest,  arch: darwin-arm64, ext: "", family: codex, harness: codex-sandbox, sandbox: "on", expect: '["seatbelt"]' }
          # codex-nono (#29) — the same real Codex CLI, confined by nono's *published* profile
          # instead of its own sandbox: an ordinary matrix row on the unchanged baseline-normalized
          # methodology, read directly against codex-sandbox above. Every restriction is the
          # vendor's — the launcher passes `nono run --profile <id> --` and nothing else, and the
          # probe, Codex's state and the report all live inside a path the profile already grants
          # rather than one we opened. Its own harness identity, tagging the resolved profile
          # version, so it forms its own time series. THIS ROW INSTALLS A REGISTRY PACK, which
          # splices Codex plugin wiring into ~/.codex; removal is verified on exit whether the run
          # succeeds or fails (scripts/nono-pack.sh).
          # Best-effort until a real run has been observed: it is unconfirmed whether the profile's
          # network policy lets Codex reach the stubbed model API on loopback. If it does not, that
          # is a finding to record about the profile — NOT something to fix by adding a network flag
          # of our own, which would make the row measure our configuration. Promote to blocking once
          # a scheduled run shows it green.
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: codex, harness: codex-nono, nono_profile: nolabs-ai/codex, expect: '["landlock","seccomp-notify","seccomp-filter","no-new-privs"]', optional: true }
          - { os: macos, runner: macos-latest,  arch: darwin-arm64, ext: "", family: codex, harness: codex-nono, nono_profile: nolabs-ai/codex, expect: '["seatbelt"]', optional: true }
          # gemini — real agent, model stubbed; as-is vs its sandbox (Seatbelt on macOS, container on Linux).
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: gemini, harness: gemini }
          # gemini-docker re-execs the whole CLI in a container; the mock is reached and auth injected
          # across that boundary via SANDBOX_FLAGS in the gemini stub runner.
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: gemini, harness: gemini-docker,       backend: docker,       expect: '["docker"]' }
          - { os: macos, runner: macos-latest,  arch: darwin-arm64, ext: "", family: gemini, harness: gemini }
          - { os: macos, runner: macos-latest,  arch: darwin-arm64, ext: "", family: gemini, harness: gemini-sandbox-exec, backend: sandbox-exec, expect: '["seatbelt"]' }
          # opencode / goose / pi / gptme / cline — real agents (chat/completions), model stubbed; NO native sandbox (unconfined "as-is").
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: opencode, harness: opencode }
          - { os: macos, runner: macos-latest,  arch: darwin-arm64, ext: "", family: opencode, harness: opencode }
          - { os: windows, runner: windows-latest, arch: windows-amd64, ext: ".exe", family: opencode, harness: opencode }
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: goose,    harness: goose }
          - { os: macos, runner: macos-latest,  arch: darwin-arm64, ext: "", family: goose,    harness: goose }
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: pi,       harness: pi }
          - { os: macos, runner: macos-latest,  arch: darwin-arm64, ext: "", family: pi,       harness: pi }
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: gptme,    harness: gptme }
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: cline,    harness: cline }
          - { os: macos, runner: macos-latest,  arch: darwin-arm64, ext: "", family: cline,    harness: cline }
          # trae (bytedance/trae-agent) — open-source agent, model stubbed via the OpenAI Responses API.
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: trae,     harness: trae }
          - { os: macos, runner: macos-latest,  arch: darwin-arm64, ext: "", family: trae,     harness: trae }
          # trae-docker — trae's own Docker execution mode (agent-driven container). Heavy (source clone +
          # pyinstaller tool-bundling per run) and only re-detects docker, so best-effort.
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: trae,     harness: trae-docker, sandbox: "docker", expect: '["docker"]', optional: true }
          # sandbox runtimes — no agent, no model; each restricts the seeded parent's own filesystem
          # by policy, so the run is a genuine child of it. The rootfs-swapping runtimes (docker,
          # podman, bwrap, nspawn, gvisor) are retired: they were never nested in the seeded parent,
          # and any sharing flags this repo adds to nest them would be its own configuration choice
          # rather than a vendor's. See the retirement note in README.md.
          # spec #5.3 (#32): none of the three is launched with a restriction this repo invented —
          # firejail gets --quiet and its own default profile, nono keeps only the grants it needs
          # to start, srt keeps its deny-by-default settings widened for writes. Network egress is
          # open on all three, as it is out of the box, so their rows read more exposed than before.
          # spec #6: as for claude-sandbox — same bwrap engine, wrapper name now resolves.
          # srt on Linux has never reported the wrapper name: all six published runs say
          # "unknown" plus user-namespace, seccomp-filter and no-new-privs. That is the
          # limitation ADR 0002 describes — the wrapper name is an INFERENCE from the
          # process ancestry, while the mechanisms are kernel-attested — and it is the
          # same shape as upstream sandbox-probe#38. srt does engage a sandbox, provably;
          # the probe simply cannot name it. Accept the kernel-enforcement signals, as
          # codex-sandbox already does on this runner.
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: sandbox, harness: srt,      runtime: srt,      expect: '["bubblewrap","user-namespace","seccomp-filter","no-new-privs"]' }
          - { os: macos, runner: macos-latest,  arch: darwin-arm64, ext: "", family: sandbox, harness: srt,      runtime: srt,      expect: '["seatbelt"]' }
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: sandbox, harness: firejail, runtime: firejail, expect: '["firejail"]' }
          - { os: linux, runner: ubuntu-latest, arch: linux-amd64,  ext: "", family: sandbox, harness: nono,     runtime: nono,     expect: '["landlock","seccomp-notify","seccomp-filter","no-new-privs"]' }
          - { os: macos, runner: macos-latest,  arch: darwin-arm64, ext: "", family: sandbox, harness: nono,     runtime: nono,     expect: '["seatbelt"]' }
    runs-on: ${{ matrix.runner }}
    env:
      PROBE: ./sandbox-probe${{ matrix.ext }}
      REPORT: reports/${{ matrix.os }}-${{ matrix.harness }}.json
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/download-artifact@v7
        with:
          name: sandbox-probe-${{ matrix.arch }}
      - if: runner.os != 'Windows'
        run: chmod +x $PROBE
      - run: mkdir -p reports
        shell: bash

      # Soft-plant decoys at the probe's own seedable targets so a sandbox blocking
      # a credential is a provable result, not "n/a". Runs identically here for the
      # direct baseline AND every harness below — parity is what makes the diff mean
      # "blocked" rather than "absent". Soft: never overwrites an existing file.
      # Windows included: it used to be skipped because bash could only plant files,
      # and that is where the named-pipe catalogue applies (probe ADR 0002).
      - name: seed decoys (baseline + sandbox parity)
        shell: bash
        run: ./scripts/seed-decoys.sh "$PROBE"

      # ── agent setup: node for every agent, sandbox deps + the CLI per family ───
      - if: matrix.family != 'direct'
        uses: actions/setup-node@v7
        with: { node-version: "22" }
      - if: (matrix.family == 'claude' || matrix.family == 'codex') && runner.os == 'Linux'
        name: linux sandbox deps (bwrap)
        shell: bash
        run: |
          sudo apt-get update || true; sudo apt-get install -y bubblewrap socat
          # Ubuntu 24.04+ blocks unprivileged user namespaces by default; bwrap needs them.
          if [ "$(sysctl -n kernel.apparmor_restrict_unprivileged_userns 2>/dev/null || echo 0)" = "1" ]; then
            sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
          fi
      - if: matrix.family == 'claude'
        run: npm install -g @anthropic-ai/claude-code && claude --version
        shell: bash
      - if: matrix.family == 'codex'
        run: npm install -g @openai/codex && codex --version
        shell: bash
      - if: matrix.family == 'gemini'
        run: npm install -g @google/gemini-cli && gemini --version
        shell: bash
      - if: matrix.family == 'opencode'
        run: npm install -g opencode-ai && opencode --version
        shell: bash
      - if: matrix.family == 'goose'
        name: install goose
        shell: bash
        run: |
          curl -fsSL https://github.com/block/goose/releases/download/stable/download_cli.sh | CONFIGURE=false bash
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
      - if: matrix.family == 'pi'
        run: npm install -g @earendil-works/pi-coding-agent && pi --version
        shell: bash
      - if: matrix.family == 'gptme'
        name: install gptme
        shell: bash
        run: |
          pipx install gptme
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
      - if: matrix.family == 'cline'
        name: install cline
        shell: bash
        run: npm install -g cline || { sleep 10; npm install -g cline; } && cline --version
      - if: matrix.family == 'trae'
        name: install trae-agent
        shell: bash
        run: |
          command -v uv >/dev/null || { curl -LsSf https://astral.sh/uv/install.sh | sh; echo "$HOME/.local/bin" >> "$GITHUB_PATH"; }
          export PATH="$HOME/.local/bin:$PATH"
          git clone --depth 1 https://github.com/bytedance/trae-agent "$RUNNER_TEMP/trae-src"
          cd "$RUNNER_TEMP/trae-src"
          uv sync --python 3.12
          uv pip install docker pexpect   # docker-mode manager imports these at startup (an extra)
          echo "TRAE_SRC=$RUNNER_TEMP/trae-src" >> "$GITHUB_ENV"
          echo "TRAE_CLI=$RUNNER_TEMP/trae-src/.venv/bin/trae-cli" >> "$GITHUB_ENV"
          "$RUNNER_TEMP/trae-src/.venv/bin/trae-cli" --version
      - if: matrix.harness == 'trae-docker'
        run: docker info
      - if: matrix.backend == 'docker'
        run: docker info

      # ── sandbox-runtime installs (family=sandbox) ──────────────────────────────
      - if: matrix.runtime == 'srt'
        name: install srt (+ bwrap deps on linux)
        shell: bash
        run: |
          npm install -g @anthropic-ai/sandbox-runtime && srt --version
          if [ "$RUNNER_OS" = "Linux" ]; then
            sudo apt-get update || true; sudo apt-get install -y bubblewrap socat ripgrep
            if [ "$(sysctl -n kernel.apparmor_restrict_unprivileged_userns 2>/dev/null || echo 0)" = "1" ]; then
              sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
            fi
          else
            brew install ripgrep || true
          fi
      - if: matrix.runtime == 'firejail'
        run: sudo apt-get update || true; sudo apt-get install -y firejail
      - if: matrix.runtime == 'nono' || matrix.harness == 'codex-nono'
        name: install nono
        shell: bash
        run: |
          if [ "$RUNNER_OS" = "Linux" ]; then
            curl -fsSL https://nono.sh/install.sh | sh
            echo "$HOME/.local/bin" >> "$GITHUB_PATH"
            # nono's seccomp-notify supervisor may need unprivileged user namespaces (gated on 24.04).
            if [ "$(sysctl -n kernel.apparmor_restrict_unprivileged_userns 2>/dev/null || echo 0)" = "1" ]; then
              sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
            fi
          else
            brew install nono
          fi

      # ── run the probe under the harness (model stubbed by the general mock) ────
      - if: matrix.family == 'direct'
        name: probe (direct)
        shell: bash
        run: $PROBE scan --tasksets baseline --tags "os=${{ matrix.os }},harness=direct" --output_path "$REPORT"

      - if: matrix.family == 'claude'
        name: probe (${{ matrix.harness }})
        timeout-minutes: 12
        shell: bash
        env:
          OUT: ${{ env.REPORT }}
          RUNNER: ${{ matrix.runner }}
          CLAUDE_SANDBOX: ${{ matrix.sandbox }}
        run: ./scripts/run-probe-via-claude-stub.sh

      - if: matrix.family == 'codex'
        name: probe (${{ matrix.harness }})
        timeout-minutes: 12
        shell: bash
        env:
          OUT: ${{ env.REPORT }}
          RUNNER: ${{ matrix.runner }}
          CODEX_SANDBOX: ${{ matrix.sandbox }}
          # Only the codex-nono row sets this; it swaps Codex's own sandbox for the declared profile.
          CODEX_NONO_PROFILE: ${{ matrix.nono_profile }}
        run: ./scripts/run-probe-via-codex-stub.sh

      - if: matrix.family == 'gemini'
        name: probe (${{ matrix.harness }})
        timeout-minutes: 12
        shell: bash
        env:
          OUT: ${{ env.REPORT }}
          RUNNER: ${{ matrix.runner }}
          GEMINI_SANDBOX: ${{ matrix.backend }}
        run: ./scripts/run-probe-via-gemini-stub.sh

      - if: matrix.family == 'opencode'
        name: probe (opencode)
        timeout-minutes: 12
        shell: bash
        env:
          OUT: ${{ env.REPORT }}
          RUNNER: ${{ matrix.runner }}
        run: ./scripts/run-probe-via-opencode-stub.sh

      - if: matrix.family == 'goose'
        name: probe (goose)
        timeout-minutes: 12
        shell: bash
        env:
          OUT: ${{ env.REPORT }}
          RUNNER: ${{ matrix.runner }}
        run: ./scripts/run-probe-via-goose-stub.sh

      - if: matrix.family == 'pi'
        name: probe (pi)
        timeout-minutes: 12
        shell: bash
        env:
          OUT: ${{ env.REPORT }}
          RUNNER: ${{ matrix.runner }}
        run: ./scripts/run-probe-via-pi-stub.sh

      - if: matrix.family == 'gptme'
        name: probe (gptme)
        timeout-minutes: 12
        shell: bash
        env:
          OUT: ${{ env.REPORT }}
          RUNNER: ${{ matrix.runner }}
        run: ./scripts/run-probe-via-gptme-stub.sh

      - if: matrix.family == 'cline'
        name: probe (cline)
        timeout-minutes: 12
        shell: bash
        env:
          OUT: ${{ env.REPORT }}
          RUNNER: ${{ matrix.runner }}
        run: ./scripts/run-probe-via-cline-stub.sh

      - if: matrix.family == 'trae'
        name: probe (${{ matrix.harness }})
        timeout-minutes: 15
        shell: bash
        env:
          OUT: ${{ env.REPORT }}
          RUNNER: ${{ matrix.runner }}
          TRAE_DOCKER: ${{ matrix.sandbox == 'docker' && 'on' || 'off' }}
        run: ./scripts/run-probe-via-trae-stub.sh

      - if: matrix.family == 'sandbox'
        name: probe (${{ matrix.harness }})
        timeout-minutes: 12
        shell: bash
        env:
          OUT: ${{ env.REPORT }}
          RUNNER: ${{ matrix.runner }}
          RUNTIME: ${{ matrix.runtime }}
        run: ./scripts/run-probe-in-sandbox.sh

      # Remove what the seed step planted, on every row, INCLUDING when the scan
      # step above failed or timed out — `always()`. A process decoy and a Windows
      # pipe server are live artifacts, so a reused or self-hosted runner would
      # otherwise accumulate them. Removes only recorded/decoy-content artifacts.
      - name: remove decoys (even if the scan failed)
        if: always()
        shell: bash
        run: ./scripts/seed-decoys.sh --cleanup "$PROBE"

      # ── assert confinement engaged (any row that declares `expect`) ────────────
      # `expect` is a list of accepted sandbox_detection values; the row passes if the probe emitted
      # any one of them. Rows also accept mechanism values (no-new-privs / seccomp-filter / landlock)
      # where the wrapper name could not always be fingerprinted; the two rows spec #6 corrected
      # (claude-sandbox and srt on Linux) now assert the wrapper name itself.
      #
      # FINDING (spec #6 AC: report, do not quietly adjust): nono on Linux is a third harness whose
      # badge may move. Its setup relaxes kernel.apparmor_restrict_unprivileged_userns, i.e. it runs
      # in a user namespace, and no nono-specific wrapper detector exists — so the broadened uid_map
      # rule can now label it `bubblewrap` where it previously read `unknown`. That is the mislabel
      # the spec accepts, not a miss; this row's mechanism-only `expect` still passes either way, so
      # it is left as-is pending a scheduled run that shows what nono actually reports.
      #
      # FINDING (spec #5.3, #32: record, do not preserve the old assertion): firejail no longer gets
      # this repo's --seccomp, so the kernel mechanisms reported beside its badge are now whatever
      # firejail's own default profile installs — `seccomp-filter` is no longer guaranteed to appear,
      # and if it does, that is firejail's default profile rather than our flag. That is a finding
      # about the tool. This row's expectation is therefore the vendor default and nothing more: the
      # `firejail` wrapper name, which is read from process ancestry and unaffected by the filter.
      - if: matrix.expect != ''
        name: assert sandbox engaged
        shell: bash
        env:
          EXPECT: ${{ matrix.expect }}
        run: |
          echo "sandbox_detection accepted in $EXPECT for $REPORT"
          jq --argjson ok "$EXPECT" -e 'any(.findings[];
            .findingType == "sandbox_detection"
            and .task == "baseline_sandbox_detector"
            and (.value as $v | $ok | index($v)))' "$REPORT"

      - name: summary
        if: always()
        shell: bash
        run: |
          {
            echo "### ${{ matrix.harness }} (${{ matrix.os }})"
            echo "- report: \`${{ env.REPORT }}\`"
          } >> "$GITHUB_STEP_SUMMARY"

      # Just this row's report, not the whole reports/ directory: reports/ is also where the
      # stored fixtures live in this repo, and uploading the directory would republish them
      # as if they were results from this run.
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: report-${{ matrix.os }}-${{ matrix.harness }}
          path: ${{ env.REPORT }}
          if-no-files-found: ignore

  # Declared-versus-actual (#27), not baseline-versus-sandbox: this produces an attestation for a
  # declared nono registry profile, never a matrix row, and touches nothing in the 0–8 exposure
  # scale. Only the OS-level policy half of the pack is attested — the pack's Codex plugin wiring is
  # inert when the sandboxed command is the bare probe (docs/attestation-mapping.md).
  #
  # Its own job, and deliberately here rather than in the per-push smoke suite, because it INSTALLS
  # A NONO REGISTRY PACK: that splices Codex plugin wiring into ~/.codex on the runner, on install,
  # even under --dry-run. It is gated exactly like every other side-effecting job in this workflow
  # (dispatch / matrix branch / weekly), and the removal is verified rather than assumed.
  #
  # PROFILE is an input, not a constant: dispatch this against any other registry profile and it
  # attests that one with no code change.
  attest:
    name: attest (${{ inputs.profile || 'nolabs-ai/codex' }})
    needs: build
    runs-on: ubuntu-latest
    env:
      PROBE: ./sandbox-probe
      PROFILE: ${{ inputs.profile || 'nolabs-ai/codex' }}
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/download-artifact@v7
        with: { name: sandbox-probe-linux-amd64 }
      - run: chmod +x sandbox-probe
      - uses: actions/setup-node@v7
        with: { node-version: "22" }
      - name: install nono
        shell: bash
        run: |
          curl -fsSL https://nono.sh/install.sh | sh
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          # nono's seccomp-notify supervisor may need unprivileged user namespaces (gated on 24.04).
          if [ "$(sysctl -n kernel.apparmor_restrict_unprivileged_userns 2>/dev/null || echo 0)" = "1" ]; then
            sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
          fi

      # The one integration check the spec asks for, and the reason this job is gated: the failure
      # mode is silent mutation of a real machine, so cleanup-after-a-crash is *exercised*, not
      # asserted. NONO_PACK_FAIL_AFTER_INSTALL aborts the run in the window where the pack exists;
      # the run must still fail AND still leave the agent configuration exactly as it found it.
      # It runs before the real attestation so a broken cleanup is caught while nothing else has run.
      - name: cleanup is verified even when the run fails part-way
        shell: bash
        env:
          NONO_PACK_FAIL_AFTER_INSTALL: "1"
          OUT: ${{ runner.temp }}/discarded-attestation.json
        run: |
          set +e
          out="$(WORK="$RUNNER_TEMP/fault" ./scripts/attest-profile.sh 2>&1)"; status=$?
          set -e
          echo "$out"
          [ "$status" -ne 0 ] || { echo "::error::fault injection did not fail the run"; exit 1; }
          grep -q 'nono-pack: verified' <<<"$out" \
            || { echo "::error::cleanup did not verify after a mid-run failure"; exit 1; }
          if grep -q 'nono:' "$HOME/.codex/config.toml" 2>/dev/null; then
            echo "::error::a nono fence block is still spliced into ~/.codex/config.toml"; exit 1
          fi

      # Immediately after the failed run above, with no reset in between: this is also the
      # repeat-run check. It must succeed without assuming the pack install was idempotent or that
      # the previous run left a clean state — nono_pack_install removes before it pulls.
      - name: attest the profile
        shell: bash
        env:
          OUT: site/attestation.json
        run: WORK="$RUNNER_TEMP/attest" ./scripts/attest-profile.sh

      # attest-profile.sh seeds its own baseline through the same script, so it
      # leaves the same live artifacts behind — cleaned up here for the same
      # reason as in the scan rows, and on the same failed-run terms.
      - name: remove decoys (even if the attestation failed)
        if: always()
        shell: bash
        run: ./scripts/seed-decoys.sh --cleanup "$PROBE"

      - name: summary
        if: always()
        shell: bash
        run: |
          {
            echo "### attestation — $PROFILE"
            jq -r '"- profile: `\(.profile.id)@\(.profile.version)`",
                   "- coverage: \(.coverage.attested)/\(.coverage.declared) declared units attestable",
                   "- gaps (reachable, undeclared): \(.gaps | length)"' site/attestation.json || true
          } >> "$GITHUB_STEP_SUMMARY"

      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: attestation
          path: site/attestation.json
          if-no-files-found: ignore

  # One aggregation job — collect every scan's report, append one immutable commit
  # to the data branch, rebuild the concatenated all-reports.json from the whole
  # history, and deploy the static site. `needs: scan` + `if: always()` so a partial
  # matrix still publishes what succeeded (no 30-way git-push race — one commit here).
  aggregate:
    name: aggregate & publish
    needs: [build, scan, attest]
    if: always()
    runs-on: ubuntu-latest
    permissions:
      contents: write # push snapshots to the data branch
      pages: write # deploy the site
      id-token: write # deploy-pages OIDC
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v7.0.1 # main: site/ + scripts/
      - uses: actions/setup-node@v7
        with: { node-version: "22" }
      - uses: actions/download-artifact@v7
        with:
          pattern: report-*
          path: incoming
          merge-multiple: true
      # The observed attestation replaces the checked-in fixture-built one, so the site's
      # attestation view shows this run's verdict. Its own `source` field says which it is, so a
      # missing artifact (attest job skipped or failed) just publishes the fixture document as
      # before, labelled as such — never an observed run's verdict by mistake.
      - uses: actions/download-artifact@v7
        continue-on-error: true
        with: { name: attestation, path: incoming-attestation }
      - shell: bash
        run: cp incoming-attestation/attestation.json site/attestation.json 2>/dev/null || true
      - name: append run to data branch + build site payload
        shell: bash
        env:
          DATA_BRANCH: gh-pages-data
        run: ./scripts/publish-site.sh
      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with: { path: public }
      - id: deploy
        uses: actions/deploy-pages@v5
